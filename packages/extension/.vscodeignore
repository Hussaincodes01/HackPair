# TypeScript source & build tooling — the runtime is bundled into dist/
src/**
build.js
tsconfig.json
**/*.ts
**/*.map

# Repo / packaging artifacts
.gitignore
.vscodeignore
.vscode/**
**/*.vsix
data/**

# Monorepo: exclude sibling packages and root git dir from git-ls-files
../**
../../**
